#include<stdio.h>
#include<math.h>
int main()
{
    float x1,x2,y1,y2;float c;
    scanf("%f %f",&x1,&y1);
    scanf("%f %f",&x2,&y2);
    float k=x2-x1;float k1=(y2-y1);
    c=pow(k,2)+pow(k1,2);
    float d=sqrt(c);
    printf("%.4f\n",d);
    return 0;
}
